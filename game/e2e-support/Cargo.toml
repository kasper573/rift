[package]
name = "e2e-support"
version.workspace = true
edition.workspace = true
description = "Shared end-to-end harness for the server and client suites: headless Chrome automation, a Keycloak admin client, and stack/asset helpers."

[dependencies]
headless_chrome = { version = "1", features = ["offline"] }
serde_json.workspace = true
ureq = { workspace = true, features = ["rustls", "json"] }

[package.metadata.dist]
dist = false
