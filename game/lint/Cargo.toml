[package]
name = "lint"
version.workspace = true
edition.workspace = true
description = "Custom lint rules for the game crates"

[dependencies]
cargo_metadata = "0.19"
serde_json.workspace = true
syn = { workspace = true, features = ["visit"] }
