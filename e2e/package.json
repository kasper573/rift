{
  "name": "rift-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end suite: drives the deployed stack through real browsers and asserts on the pixels the game renders.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@types/node": "^22.10.0",
    "@types/pngjs": "^6.0.5",
    "pngjs": "^7.0.0"
  }
}
